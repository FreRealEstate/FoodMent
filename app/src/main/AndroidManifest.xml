<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.czareg.foodment">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuGlowne" />
        <activity android:name=".Lodowka" />
        <activity android:name=".Przepisy" />
        <activity android:name=".Dieta" />
        <activity android:name=".ListaPrzepisow" />
        <activity android:name=".Przepis" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ListaZakupow"></activity>
    </application>

</manifest>